meta {
  name: Applications
  seq: 2
}

auth {
  mode: inherit
}

script:pre-request {
  const Altinn = require(bru.cwd() + '/pre_request.js');
  
  await (new Altinn(req)).Authenticate({
      "tokenType": "platform",
      "app": "studio.designer"
  });
  
}
