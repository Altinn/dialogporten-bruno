meta {
  name: Register
  seq: 6
}

auth {
  mode: inherit
}

script:pre-request {
  await (new Altinn(req)).Authenticate({
      "tokenType": "enterprise",
      "orgNo": "991825827",
      //"org": "digdir",
      "scopes": "altinn:register/partylookup.admin"
  });
}
