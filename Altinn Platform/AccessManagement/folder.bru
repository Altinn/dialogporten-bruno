meta {
  name: AccessManagement
  seq: 7
}

auth {
  mode: inherit
}

script:pre-request {
  const Altinn = require(bru.cwd() + '/pre_request.js');
  
  await (new Altinn(req)).Authenticate({
      "tokenType": "enterprise",
      "orgNo": "991825827",
      // "org": "digdir",
      "scopes": "altinn:accessmanagement/authorizedparties.admin"
  });
  
}
